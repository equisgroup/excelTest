@model IEnumerable<ExcelResourceManager.Core.Models.Conflicto>

@{
    ViewData["Title"] = "Conflictos";
}

<div class="row">
    <div class="col-12">
        <h2>Gestión de Conflictos</h2>

        @if (TempData["Success"] != null)
        {
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                @TempData["Success"]
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        }

        <div class="card">
            <div class="card-body">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Tipo</th>
                            <th>Nivel</th>
                            <th>Empleado</th>
                            <th>Fecha</th>
                            <th>Descripción</th>
                            <th>Estado</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var conflicto in Model)
                        {
                            <tr>
                                <td>@conflicto.Id</td>
                                <td>@conflicto.Tipo</td>
                                <td>
                                    @switch (conflicto.Nivel)
                                    {
                                        case ExcelResourceManager.Core.Enums.NivelConflicto.Critico:
                                            <span class="badge bg-danger">Crítico</span>
                                            break;
                                        case ExcelResourceManager.Core.Enums.NivelConflicto.Alto:
                                            <span class="badge bg-warning text-dark">Alto</span>
                                            break;
                                        case ExcelResourceManager.Core.Enums.NivelConflicto.Medio:
                                            <span class="badge bg-info">Medio</span>
                                            break;
                                        case ExcelResourceManager.Core.Enums.NivelConflicto.Bajo:
                                            <span class="badge bg-secondary">Bajo</span>
                                            break;
                                        case ExcelResourceManager.Core.Enums.NivelConflicto.Informativo:
                                            <span class="badge bg-light text-dark">Informativo</span>
                                            break;
                                    }
                                </td>
                                <td>@conflicto.EmpleadoId</td>
                                <td>@conflicto.FechaConflicto.ToString("dd/MM/yyyy")</td>
                                <td>@conflicto.Descripcion</td>
                                <td>
                                    @if (conflicto.Resuelto)
                                    {
                                        <span class="badge bg-success">Resuelto</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger">Pendiente</span>
                                    }
                                </td>
                                <td>
                                    @if (!conflicto.Resuelto)
                                    {
                                        <form asp-action="Resolver" asp-route-id="@conflicto.Id" method="post" style="display:inline;">
                                            <button type="submit" class="btn btn-sm btn-success">Resolver</button>
                                        </form>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
